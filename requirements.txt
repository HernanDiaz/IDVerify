# PyTorch con soporte CUDA 13.0 (RTX 5060 / Blackwell)
# Instalar con:
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu124
# Nota: CUDA 13.0 es retrocompatible con binarios CUDA 12.4 de PyTorch
torch>=2.3
torchvision>=0.18

# HPO
optuna>=3.0

# Data / ML
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
scipy>=1.11
tqdm>=4.65

# Imágenes
Pillow>=10.0
