%% ============================================================
%% DocVerify — Pattern Recognition Letters (PRL) submission
%%
%% Format: elsarticle, 5p two-column, author-year (elsarticle-harv)
%% Style:  prletters.sty as required by the journal
%% Limit:  7 pages including references
%%
%% Build:
%%   pdflatex main.tex
%%   bibtex main
%%   pdflatex main.tex
%%   pdflatex main.tex
%% ============================================================

\documentclass[final,5p,times,twocolumn,number]{elsarticle}

%% ── Packages ─────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{url}
\usepackage{microtype}

%% ── PRL journal style (required by the journal) ──────────────
\usepackage{prletters}
\usepackage{etoolbox}

%% Make abstract span full width (remove Article history / keywords panel)
\makeatletter
\renewcommand{\articleinfobox}{}
\patchcmd{\MaketitleBox}%
  {\parbox[t]{.4\textwidth}{\unvbox\absbox}}%
  {\parbox[t]{.777\textwidth}{\unvbox\absbox}}%
  {}{}
\patchcmd{\pprintMaketitle}%
  {\parbox[t]{.4\textwidth}{\unvbox\absbox}}%
  {\parbox[t]{.777\textwidth}{\unvbox\absbox}}%
  {}{}
\renewenvironment{abstract}{%
  \global\setbox\absbox=\vbox\bgroup%
  \hsize=.777\textwidth}%
 {\par\vspace{6pt}%
  \noindent\mbox{~}\hfill\copyright~\the\year\
  Elsevier~Ltd.~All~rights~reserved.\egroup}
\makeatother

%% ── hyperref ─────────────────────────────────────────────────
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}

%% ── Convenience macros ───────────────────────────────────────
\DeclareMathOperator*{\argmin}{arg\,min}
\newcommand{\TODO}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

%% Round-1 (first major revision) changes are now ACCEPTED: \rev renders as
%% normal text. Round-2 (this minor revision) changes are highlighted in blue
%% via \revB, the same colour used in the previous round, as the journal requires.
\newcommand{\rev}[1]{#1}
\newcommand{\revB}[1]{\textcolor{blue}{#1}}
% Camera-ready: also make \revB plain -> \newcommand{\revB}[1]{#1}

%% ── Graphics path ────────────────────────────────────────────
\graphicspath{{figures/}}

%% Journal declaration
\journal{Pattern Recognition Letters}

% ============================================================
\begin{document}

\begin{frontmatter}

\title{Multi-objective Pareto hyperparameter optimization for joint detection and
       localization of forged identity documents}

\author[1]{Hern\'{a}n D\'{i}az Rodr\'{i}guez\corref{cor1}}
\cortext[cor1]{Corresponding author.}
\ead{diazhernan@uniovi.es}

\address[1]{Department of Computer Science, University of Oviedo, Asturias, Spain}

\begin{abstract}
Automated forensic analysis of identity documents requires simultaneously
detecting whether a document has been tampered with and identifying which
regions were altered. These two objectives are structurally coupled but exist
in tension: optimizing one with a fixed loss weight may degrade the other.
We propose DocVerify, a multi-task convolutional network paired with a
multi-objective hyperparameter optimization (HPO) framework that treats
detection quality (PR-AUC) and localization quality (Dice) as simultaneous
objectives. Using the Multiobjective Tree-structured Parzen Estimator (MOTPE)
within a 10-fold nested cross-validation protocol (50 trials per outer fold,
500 unique configurations evaluated across 5 inner splits, totaling 2\,500
individual model trainings), DocVerify selects configurations from the
Pareto-optimal front by minimizing Euclidean distance to the ideal point
(1, 1), without any manual weighting. Evaluated on the FantasyID benchmark,
DocVerify achieves PR-AUC = 0.9921 ± 0.0058 and
Dice = 0.856 ± 0.030 under nested cross-validation, and detection
F1@0.5 = 0.969 ± 0.014 with localization F1 = 0.807 ± 0.096 on the
blind test set (30 seeds). An ablation confirms that Pareto-optimized loss
weighting provides a statistically significant Dice improvement over
equal-weight joint training (p < 0.001, d = 1.01, large effect)
while maintaining equivalent detection accuracy. \revB{These figures characterize
in-domain performance on FantasyID; zero-shot transfer to the out-of-domain SIDTD
benchmark falls to near chance (PR-AUC 0.555), so the reported accuracy should be
read as bounded to the training domain.}
\end{abstract}

\begin{keyword}
  identity document forensics \sep
  multi-objective optimization \sep
  hyperparameter optimization \sep
  multi-task learning \sep
  document forgery detection
\end{keyword}

\end{frontmatter}

% ============================================================
% SECTIONS
% ============================================================

\input{sections/01_introduction}

\input{sections/02_related_work}

\input{sections/03_methodology}

\input{sections/04_experiments}

\input{sections/05_discussion}

\input{sections/06_conclusion}

% ============================================================
% BIBLIOGRAPHY
% ============================================================

\bibliographystyle{elsarticle-num-names}
\bibliography{references}

\end{document}
